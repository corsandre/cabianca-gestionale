services:
  web:
    build: .
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./app/static/uploads:/app/app/static/uploads
    env_file:
      - .env
    restart: unless-stopped
